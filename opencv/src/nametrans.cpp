#include <stdio.h>
#include <string>

for(int i=0;i<500;i++)
{
    string str1 = "./data/Capture";
    char* name;
    sprintf(name,"%d",i);
    string str2 = name;
    string str = str1 + str2 + ".BMP";
}

